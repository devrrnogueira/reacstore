{
  "name": "reacstore",
  "version": "0.1.0",
  "private": true,
  "main": "dist/reacstore.js",
  "devDependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "test": "react-scripts start",
    "build": "rm -rf dist && cp -r src/lib dist"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
